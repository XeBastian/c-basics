#include <stdio.h>

int main(){
    int primeNumbers[2] ={1,2}; 
    // basic array declarations above
    // looping through an array
    for (int i = 0; i <= 2; i++){
        printf("The current iteration has %d as its number\n", i);
    }
    return 0;
}
